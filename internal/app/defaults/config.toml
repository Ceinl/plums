# Stock fallback layout. `split` is provided by the user plugin in config.go;
# the compiled config selects it, so this only applies to a stock binary.
default_layout = "chat"
hide_thinking = true
# Hide pre-existing backend sessions; only sessions created in this run are shown.
clear_history = false

# Backend provider: "opencode" or "codex".
[backend]
provider = "opencode"

# Opencode server URL. When omitted, plums picks a port automatically from
# your project directory so different worktrees don't collide.
# [opencode]
# opencode_server_url = "http://127.0.0.1:4096"

[split]
left_width = 50

[harnesses]
opencode_server = "*"
